[project]
name = "multi-agent-research-system"
version = "0.1.0"
description = "CCA-F Scenario 3 -- one coordinator, four subagents. The coordinator holds Task and nothing else."
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.2.128",
    "python-dotenv>=1.0.0",
    "flask>=3.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.setuptools]
packages = []

[tool.uv]
package = false
